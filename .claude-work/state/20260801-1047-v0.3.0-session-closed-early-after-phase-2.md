---
created: 2026-08-01T10:47:38+12:00
title: v0.3.0 re-baseline closed early after phase 2
tags: [spec-design, duckdb, storage, serialization, product-strategy, ws-close]
summary: Session 20260731-0930 closed at the user's request with 2 of 6 phases done. Phase 1 archived the 0.2.0 docs; phase 2 grew from "fill a matrix" into a full type-system redesign. Phases 3-6 (direction doc, README/CLAUDE.md, held codegen goal, roadmap) are untouched but their plan text has been updated to the new decisions.
---

## Where things stand

Session **closed early**, deliberately. Phases 1–2 done and committed; phases
3–6 not started. `.active` removed.

**Read first when resuming:**
`.claude-work/notes/20260731-1552-dbdict-type-system-decisions.md` — the
authority on the type system, 15 logged decisions. `goal.md` is the brainstorm
record and has been corrected in place; the 1253 capability-matrix note is the
authority on per-target capability but is *partly superseded* and says so in a
banner.

## The type system, settled

**10 dbdict types** (the terms *lingua franca* / *LF* are retired and swept
from every file):

```
bool  int8  int16  int32  int64  float32  float64  string  date  timestamp
```

- numerics and `bool` defined by **bit layout**; temporals by **pinned RFC
  profile**
- `date` = RFC 3339 `full-date`; `timestamp` = RFC 3339 `date-time` µs +
  optional RFC 9557 `[Zone]`
- **canonical ≠ physical**: native type where one exists (DuckDB, DuckLake,
  Postgres), lexical form where none does (SQLite, HDF5)
- gone: `decimal(p,s)` → V2+ · `timestamptz` removed · `datestamp` never
  introduced
- zones: in-value per RFC 9557, **plus** `timezone:` / `timezone_from:` as the
  required spillover on targets whose native type discards the zone
- SQLite emission is **non-STRICT**, affinities chosen deliberately
- `string` unbounded; `max_chars:` / `max_bytes:` optional constraints, the
  latter doubling as HDF5's compression switch
- **V1 targets: `duckdb` `ducklake` `sqlite` `hdf5`. `postgres` → V2.**

## What phase 2 falsified

1. the flat-cost curve across SQL targets — SQLite is affinity-typed
2. "8 of 12 types free everywhere" — three, against V1 targets
3. "SQLite comment support unverified" — no `COMMENT ON`, but DDL comments
   survive verbatim in `sqlite_schema.sql`

All three corrected in `goal.md` in-phase, struck through rather than deleted.

## Next Steps

1. **Phase 3 — the direction document** (`docs/vision-direction-0.3.0.md`).
   The biggest remaining phase. Its `impl.md` entry now carries a banner
   pointing at the 1552 decisions note. Start it on a **fresh context**.
2. Phases 4–6 follow: README/CLAUDE.md reposition · held codegen goal rewrite ·
   roadmap + triage of the six benchmark follow-ups + clean tree.
3. **Dangling link to fix**: `docs/v0.2.0/README-archive-note.md` references
   `docs/vision-direction-0.3.0.md`, which phase 3 creates.

## Open probes

- **`hdf5-temporal-compression`** — the only one that could reopen a settled
  decision. Lexical temporals are 27–61 bytes/row vs 8 for integers; "compression
  closes the gap" is `Inferred:`, not measured. Needs `h5py` (not installed).
- **`sqlitejl-temporal`** — the one unresolved driver row. Julia 1.12.6 is
  installed, SQLite.jl is not. Matters because Julia is the first codegen
  target and SQLite.jl may serialize to `BLOB`.
- `pg-type-oracle` (deferred with Postgres to V2) · `sqlite-comment-durability`
  · `jld2-h5-crosscheck`

## Relevant Files

- `.claude-work/notes/20260731-1552-dbdict-type-system-decisions.md` — **start here**
- `.claude-work/notes/20260731-1253-capability-matrix.md` — capability research, partly superseded
- `.claude-work/sessions/20260731-0930-.../summary.md` — what was and wasn't done
- `.claude-work/sessions/20260731-0930-.../impl.md` — phases 3–6 plan text updated to the new decisions
- `docs/v0.2.0/` — frozen 0.2.0 README + CLAUDE.md + archive note
- `.claude-work/insights/20260801-1047-canonical-form-can-outrun-its-physical-layers.md`

## Environment notes

Probes ran against DuckDB CLI **v1.5.4** and SQLite **3.37.2** (Python 3.10.12
stdlib). Not available on this machine: `sqlite3` CLI, `psql`, `h5dump`,
`h5py`, SQLite.jl.
